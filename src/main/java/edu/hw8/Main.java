package edu.hw8;

public class Main {

    private Main() {
    }

    /*public static void main(String[] args) throws IOException {
        var server = new Server();
        new Thread(server::run).start();
       for(int i =0; i < 2; i++) {
           var curr = new Socket("localhost",49001);
           new Thread(new ClientHandler(curr)).start();
       }

        //serv.SERVER.close();
    }*/
}
